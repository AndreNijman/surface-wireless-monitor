# ISO build artifacts — produced by build-iso.sh at runtime
build/
*.iso
*.img
*.squashfs

# Logs
*.log

# Local secrets overlay — Wi-Fi credentials, SSH keys.
# Baked into the ISO at build time; never committed (this repo is public).
overlay-local/
